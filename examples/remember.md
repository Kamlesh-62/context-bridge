# remember — Save something to project memory

## Basic — just a title

```
"Remember that we use PostgreSQL for the database"
```

## With type

```
"Remember as a decision: we chose PostgreSQL over MongoDB because our data is relational"
```

## With tags

```
"Remember as a fact tagged backend, config: API runs on port 3000"
```

## Architecture note

```
"Remember as architecture: we use a monorepo with turborepo, apps/ has the web app and API, packages/ has shared libs"
```

## Constraint

```
"Remember as a constraint: all API responses must be under 200ms, this is in our SLA"
```
